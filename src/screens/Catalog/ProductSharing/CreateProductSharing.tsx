import { useParams } from "react-router-dom";

const CreateProductSharing = () => {
    const { id } = useParams();
  return (
    <div>CreateProductSharing</div>
  )
}

export default CreateProductSharing