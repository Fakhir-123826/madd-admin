import { useParams } from "react-router-dom";

const CreateInventoryManagement = () => {
    const { id } = useParams();
  return (
    <div>CreateInventoryManagement</div>
  )
}

export default CreateInventoryManagement